<?xml version="1.0" encoding="UTF-8"?>
<entities xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:noNamespaceSchemaLocation="http://moqui.org/xsd/entity-2.1.xsd">

    <!-- Party -->
    <entity entity-name="Party" package-name="party">
        <field name="partyId" type="id" is-pk="true"/>
        <field name="partyTypeEnumId" type="id" not-null="true"/>
        <field name="createdDate" type="date-time"/>

        <relationship type="one"
                      related="moqui.basic.Enumeration"
                      fk-name="partyTypeEnumId"
                      related-field="enumId"
                      title="Party Type"/>
    </entity>

    <!-- Person -->
    <entity entity-name="Person" package-name="party">
        <field name="partyId" type="id" is-pk="true"/>
        <field name="firstName" type="text-short"/>
        <field name="lastName" type="text-short"/>
        <field name="dateOfBirth" type="date"/>

        <relationship type="one"
                      related="party.Party"
                      fk-name="partyId"/>
    </entity>

    <!-- PartyGroup -->
    <entity entity-name="PartyGroup" package-name="party">
        <field name="partyId" type="id" is-pk="true"/>
        <field name="groupName" type="text-medium"/>
        <field name="description" type="text-medium"/>

        <relationship type="one"
                      related="party.Party"
                      fk-name="partyId"/>
    </entity>

    <!-- ContactMech -->
    <entity entity-name="ContactMech" package-name="party">
        <field name="contactMechId" type="id" is-pk="true"/>
        <field name="contactMechTypeEnumId" type="id" not-null="true"/>
        <field name="infoString" type="text-medium"/>
        <field name="createdDate" type="date-time"/>

        <relationship type="one"
                      related="moqui.basic.Enumeration"
                      fk-name="contactMechTypeEnumId"
                      related-field="enumId"
                      title="Contact Type"/>
    </entity>

    <!-- PartyContactMech -->
    <entity entity-name="PartyContactMech" package-name="party">
        <field name="partyId" type="id" is-pk="true"/>
        <field name="contactMechId" type="id" is-pk="true"/>
        <field name="fromDate" type="date-time" is-pk="true"/>
        <field name="thruDate" type="date-time"/>

        <relationship type="one"
                      related="party.Party"
                      fk-name="partyId"/>

        <relationship type="one"
                      related="party.ContactMech"
                      fk-name="contactMechId"/>
    </entity>

</entities>
