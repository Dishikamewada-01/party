<?xml version="1.0" encoding="UTF-8"?>
<component name="party"
           version="1.0.0"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:noNamespaceSchemaLocation="http://moqui.org/xsd/component-1.0.xsd">

    <entity-facade>
        <!-- Entity definitions -->
        <entity-resource location="entity/PartyEntities.xml"/>

        <!-- Enum seed data -->
        <data-resource type="seed-initial" location="data/PartyEnumData.xml"/>


        <!-- Party seed data -->
        <data-resource type="seed" location="data/PartySeedData.xml"/>
    </entity-facade>

</component>
