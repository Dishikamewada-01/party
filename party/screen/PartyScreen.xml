<?xml version="1.0" encoding="UTF-8"?>
<screen name="PartyScreen"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="http://moqui.org/xsd/screen-1.0.xsd"
        require-authentication="anonymous-all">

    <!-- TRANSITION -->
    <transition name="createParty">
        <service-call name="create#party.Party"/>
        <default-response/>
    </transition>

    <!-- ACTIONS -->
    <actions>
        <entity-find entity-name="party.Party" list="partyList"/>
    </actions>

    <!-- WIDGETS -->
    <widgets>

        <!-- CREATE PARTY -->
        <container-dialog id="CreatePartyDialog" button-text="Create Party">

            <form-single name="CreatePartyForm" transition="createParty">

                <!-- AUTO GENERATED FIELDS -->
                <auto-fields-entity entity-name="party.Party"
                                    field-type="edit"/>

                <field name="submitButton">
                    <default-field title="Create">
                        <submit/>
                    </default-field>
                </field>

            </form-single>
        </container-dialog>

        <!-- PARTY LIST -->
        <form-list name="PartyListForm" list="partyList">
            <auto-fields-entity entity-name="party.Party"
                                field-type="find-display"/>
        </form-list>

    </widgets>
</screen>
